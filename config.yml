title: POC <Bus Ticketing>
description: POC
template:
  repo: poc-lessbus-ticketinggreater-template
  name: poc-lessbus-ticketinggreater
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
